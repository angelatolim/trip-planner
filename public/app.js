// find all elements to work with
const saveIconElem = document.querySelectorAll('.save-icon')

// setup event listeners
saveIconElem.addEventListener('click', handleClick)

// event handlers
function handleClick() {
    // get trip id

    // change to save icon

    // 
}


